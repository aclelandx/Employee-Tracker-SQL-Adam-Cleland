// imports the header text from the file located in here
const headerText = require(`./headerText`)
// exports the header text information so it can be utilized elsewhere
module.exports = { headerText }